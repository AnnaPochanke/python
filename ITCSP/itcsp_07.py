text = "abcdefg"
first = text[0]
last = text[-1]
middle = text[int(len(text)/2)]
print(first + middle + last)

text = "abcdef"
first = text[0]
last = text[-1]
middle = text[int(len(text)/2)]
print(first+middle+last)
